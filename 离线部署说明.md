# 离线部署说明（未联网 Windows 机器独立运行）

本程序已改造为**完全离线、可移植**：整套文件夹复制到其他 Windows 机器后，双击启动脚本即可运行，**无需安装 Python、无需联网**。

## 目录结构

```
手机自动化填写数值软件\
├── 启动界面.bat              ← 双击这个，自动启动 GUI（离线优先用自带 Python）
├── 离线部署说明.md
├── README.md
├── .gitignore
├── python\                    ← 自带便携 Python 3.13（64 位），运行时整体迁移，无需安装
├── 依赖\                      ← 离线依赖 wheel 包（pyserial/openpyxl/xlrd/et_xmlfile）
├── firmware\                  ← ESP32 固件
└── host\                      ← 主程序（gui.py / run.py / config.json / 文档 / xlsx）
    └── 启动工控机填数助手.bat  ← host 目录内的等价启动脚本
```

## 打包方法

1. 直接把整个 `手机自动化填写数值软件` 文件夹复制到一个 **U 盘 / 移动硬盘 / 目标机任意目录** 即可。
2. 只要 `python\`、`依赖\`、`host\` 三个文件夹和 `启动界面.bat` 在一起，目录名可随意改（bat 用相对路径定位，不强依赖具体目录名）。

## 运行方法

- 在目标机（**64 位 Windows**）上双击 `启动界面.bat`。
- 启动脚本会按以下顺序工作：
  1. 优先使用自带的 `python\python.exe`（完全离线）；若没有，再回退到系统的 `py` / `python`。
  2. 进入 `host\` 目录。
  3. **依赖自检**：检测 `pyserial / openpyxl / xlrd / tkinter` 是否已可用；
     - 若已可用 → 直接启动 GUI（最常见情况，零等待）。
     - 若缺失 → 自动执行 `pip install --no-index --find-links 依赖\ ...`，**从本地 `依赖\` 文件夹离线安装**，不访问网络。
  4. 启动 `gui.py` 图形操作台。

## 依赖清单（已离线固化）

| 包 | 版本 | 用途 |
|----|------|------|
| pyserial | 3.5 | 串口通信（ESP32 串口兜底通道） |
| openpyxl | 3.1.5 | 读写 .xlsx（数值表） |
| xlrd | 2.0.2 | 读取旧版 .xls |
| et_xmlfile | 2.0.0 | openpyxl 的依赖 |
| tkinter | （Python 标准库） | GUI 界面 |

运行时真正需要的第三方库只有上述前三个；wheel 已下载在 `依赖\` 中，断网也能安装。

## 注意事项

- **仅支持 64 位 Windows**：自带的 `python\` 是 64 位 Python 3.13.2。若目标机是 32 位系统，需自行替换为 32 位 Python（并重新下载对应 `依赖\` wheel）。
- `python\` 体积约 450MB，是运行时本身，不是源码；它**不纳入 git 跟踪**（见 `.gitignore`），仅本地打包使用。
- 若目标机已装有 Python 3.10+ 且已 pip 安装上述依赖，也可不放 `python\` 文件夹，bat 会自动回退到系统 Python。

## 如需从仓库重建便携 Python（罕见）

本仓库只跟踪源码与 `依赖\` 下的 wheel，不跟踪 450MB 的 `python\`。若你只有 git 克隆、需要重建离线运行时，在本机执行：

```bat
:: 1) 复制一份干净的 Python 3.13（64 位）安装目录到 python\
xcopy /E /I /Y "C:\你的Python313路径" "python"

:: 2) 删除与本项目无关的包（可选，瘦身用）
::    仅保留：serial / openpyxl / xlrd / et_xmlfile / pip

:: 3) 用依赖\ 离线校验安装
python\python.exe -m pip install --no-index --find-links 依赖 pyserial==3.5 openpyxl==3.1.5 xlrd==2.0.2
```

完成后双击 `启动界面.bat` 即可离线运行。
