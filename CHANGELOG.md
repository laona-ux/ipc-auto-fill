# CHANGELOG · ipc-auto-fill

> 「工控机自动填数工具」发布版变更记录。
> 本项目在整理历史时**尚未纳入 git**（`git log` 于 2026-08-25 确认根目录非 git 仓库），
> 因此本变更记录依据**文件层面可核实证据**（备份包、`*.bak_*` 备份、文件时间戳、审计日志）与
> 开发过程中的事件记忆整理。**无法用文件独立佐证的条目已标注「待确认」**。
>
> 版本号约定：`v1` ＝ `host/backup_v1_20260824_155913.zip` 所代表的完整可用初版；
> 后续阶段的版本号为本整理团队建议（见 `docs/版本整理说明.md`），非既有正式 tag。

---

## v1.3（2026-08-26 · 硬件兼容、性能与 GUI 体验优化）

- 完成 Pico + CH343（COM9）真实硬件链路验收：串口 ping、home 归零、绝对坐标跟踪、鼠标移动、键盘输入、Excel 读取与指令生成均通过。
- 修复 CircuitPython `adafruit_hid` 库文件 UTF-8 BOM 导致的隐藏字符 `NameError`，并同步清理项目库文件。
- 修复 CircuitPython 兼容性：移除 `decode(..., errors="replace")`，避免收到串口指令时运行时异常。
- 修复字符按键映射：不再依赖部分 HID 库不存在的 `Keycode.ord_to_keycode()`，兼容稳定版 HID 库。
- 优化串口应答读取：使用短超时与按行读取，缩短短 JSON 指令的等待时间。
- Pico 鼠标分步延时由 5ms 降至 1ms，改善大范围移动速度。
- GUI 步骤流程新增「测试所选步骤」：选中单击/双击/移动/输入等步骤后可立即单步执行，不读取 Excel；坐标动作自动归零。
- 坐标定位方向键改为实体键盘一致的 T 形布局（上、左/下/右）。
- 明确支持 CH343；COM8 等用户正在使用的其它串口不会被自动占用。

## v1.3.1（坐标稳定性修复）

- 修复手动移动工控机鼠标后，Pico 内部坐标记录与真实光标位置不同步导致的坐标偏移。
- 绝对坐标动作现在会先重新执行 `home` 物理归零，再移动到目标坐标，避免偏移累积。
- 保留 `get_pos` 坐标查询和 F8 记录功能；手动移动后再次跳转可自动校正。

---

## 未发布（2026-08-25 · 当前工作树 / 发布建议为 v1.5 或初版提交快照）

**本轮改动（`host/run.py` 为主）——按 `similar-projects-汇总报告.md` 第⑤节借鉴清单 + 前序架构/可靠性/安全三方审查结论落地：**

- 断点语义修正：**人工确认通过后**才将该行写入`.progress` 断点（原为确认前写入，用户发现错误退出后该行会被断点永久跳过导致漏填/错值）。
- 断点绑定**配置指纹**（`steps_digest`，SHA256：步骤流程 + 表头/范围 + 数据文件 mtime/size）：续传时检测「配置漂移」并警告，防止改了流程仍按旧坐标续传写错位置。
- **type 文本净化**：拦截/替换 `\n`、`\t` 及其它控制字符（防被 Pico 固件转成真实回车/Tab 触发计划外按键），并截断到 64 字符上限。
- **key 按键白名单**：仅放行导航/功能/ctrl/shift 安全组合，拦截 `win`/`alt` 等危险修饰键；生成阶段抛错 + 启动配置校验双重 fail-fast。
- **审计日志**：每次运行按行追加写入 `host/audit/fill_YYYYMMDD.jsonl`（时间/行号/汇总/确认结果/状态）。（证据：`host/audit/fill_20260825.jsonl` 318KB，2026-08-25 12:54 最后写入。）
- **`validate_config()` 启动配置校验**：步骤类型、key 白名单、button 引用、坐标整数、列引用、范围、confirm 枚举一次性报出。
- **修复 CLI 整数列 bug**（`col:2` 整数列号被误报「列不存在」，与 GUI 行为对齐）。
- 新增 **stdlib-only 单元测试套件** `host/tests/test_run.py`（34 项，全部通过；pytest 未安装，用 unittest 避免新依赖）。

> 佐证：`host/测试报告-完善后.md`（2026-08-25，含 34 项单测通过、3 项 CLI 端到端验证）、`host/tests/test_run.py`、`host/audit/fill_20260825.jsonl`。
> 未做真机硬件发送测试（`confirm="enter"` 需连串口环境实测）；GUI 复用函数签名保持向后兼容。

**测试报告：** 见 `host/测试报告-完善后.md`；单测 `python -m unittest discover -s tests -v`（34 passed）。

---

## v1.2（2026-08-24 · 当晚，GUI 体验与坐标定位阶段）

**停止/看门狗与坐标定位（F8）：**

- 修复「按『停止』后界面卡死无法恢复」：串口打开限时 3 秒（有界打开线程）、延时可被停止打断（拆 50ms 小段）、指令超时处理、**12 秒看门狗强制恢复界面**。（佐证：`gui.py` 中 `_open_link_bounded` / `_interruptible_sleep` / `_stop_watchdog`。）
- 新增**坐标定位（F8）**：归零 + 方向键移动光标 + 按 F8 把当前坐标写入所选鼠标类步骤（click/dblclick/move/press_at）。
- Pico 固件新增 `get_pos` 指令（查询当前光标坐标），作为坐标定位基础。

**界面统一字体 / 高清屏：**

- 统一字体为「微软雅黑 UI」（常规 10pt、仅标题/表头加粗），去除混杂加粗；启用 Windows 高 DPI 感知，解决文字发虚。（佐证：`gui.py` `_setup_style` 与 `main()` DPI 设置。）

**P0 六项缺陷修复 + 轻量增强（`gui.py` / `run.py`）：**

- 校准不再「展开并回写」自定义按钮结构（新增来源映射，校准只改原始目标，不再破坏 button 引用）。
- 停止看门狗除恢复界面外，还强制释放 worker 持有的串口连接（新增 `current_link` 跟踪）。
- `_open_link_bounded` 超时后回收「迟到打开成功」的僵尸串口连接（后台 reap 线程），防止端口被占。
- 空单元格的 `input` 步骤整步跳过（含 pre_keys/回车/后延时），避免把工位已有数值清掉。
- 修正断点续传 / `row_start>1` 时进度条比例到不了 100% 的问题。
- `collect_config` 非法整数输入不再裸抛 `ValueError`（`_safe_int` 化），并加输入框数字校验。
- 新增 dirty（未保存）标记 + 关闭前提示 + `Ctrl+S` 保存；全局快捷键 F8/F9/F10/空格/回车。
- 配置文件写入改为 tmp + `os.replace` 原子化，保存前留备份。

> **版本佐证**：`host/gui.py.bak_20260824_225111` 与 `host/run.py.bak_20260824_225111`（均 2026-08-24 22:51:11）为这一批修复前对 `gui.py`（当时 ~50KB）/`run.py`（当时 ~21KB）的备份，之后文件显著增大（当前 `gui.py` 84KB / `run.py` 35KB），反映上述功能累积。具体修复内容结合开发事件记忆整理，**属「待确认」细节**（缺少逐条的外部可复核记录）。

---

## v1.1（2026-08-24 · 下午，Zero 变体 + 步骤流程配置改写）

- 新增**树莓派 Zero 2W 备选方案**目录 `zero/`：`mcp_hid.py`、`run_on_zero.py`、`usb_gadget_setup.sh`、`Zero2W-使用说明.md`（Zero OTG 直接模拟 HID，可不买 CH340）。（佐证：上述文件时间戳 2026-08-24 16:53–16:55。）
- `host/config.example.json` 与 `README.md` 改写为「**步骤流程（steps）+ 自定义按钮（buttons）**」模型，取代/兼容旧 `targets` 配置：支持「点击进入某界面→填入→返回→再进另一界面→再点」的翻页录入；`README.md` 从 v1 的 18.4KB 增至 21.4KB。（佐证：`config.example.json` 含 `steps`/`buttons` 与多界面示例；README 时间戳 2026-08-24 18:31。）
- 新增 `启动界面.bat` 一键启动 GUI（2026-08-24 23:12）。

---

## v1.0（2026-08-23 ~ 2026-08-24 · 初版，可用基线）

- 立项并搭建工程骨架：`pico/boot.py`（仅暴露 HID、禁串口控制台），示例数据 `host/values.xlsx`。（佐证：`pico/boot.py` 2026-08-23 20:06，`host/values.xlsx` 2026-08-23 20:53。）
- 实现核心链路：上位机 `host/run.py`（读表→步骤引擎→断点→串口发送）与 `host/gui.py`（Tkinter 图形界面）、Pico 固件 `host/../pico/code.py`（HID 键鼠 + 串口指令循环）、`host/config.json` / `config.example.json`。
- 支持功能：Excel/CSV 读取（xlsx/xlsm/xls/csv）、列名或列序号取值、人工确认（`confirm`）、断点续传、坐标校准、`--dry-run`、`--calibrate`、`--resume`。
- **2026-08-24 15:59**：制作 `host/backup_v1_20260824_155913.zip`，备份当时的完整可用初版（README 18.4KB / run.py 20.6KB / gui.py 49.8KB / code.py 9.7KB / config）。**该备份包即 v1 基线的权威快照。**

---

## 版本证据一览（文件层面）

| 证据 | 类型 | 时间戳 | 说明 |
|---|---|---|---|
| `host/backup_v1_20260824_155913.zip` | 备份包 | 文件 2026-08-24 15:59:14；内含 README/code.py 8-24 15:26, run.py 15:20, gui.py 15:27 等 | v1 完整初版快照 |
| `host/gui.py.bak_20260824_225111` | 备份 | 2026-08-24 22:51:11 | P0 批量修复前的 GUI |
| `host/run.py.bak_20260824_225111` | 备份 | 2026-08-24 22:51:11 | P0 批量修复前的 CLI |
| `host/audit/fill_20260825.jsonl` | 审计日志 | 2026-08-25 12:54 最后写入 | 验证 2026-08-25 完善轮 |
| `host/测试报告-完善后.md` | 文档 | 2026-08-25 | 6 项完善 + 34 项单测 |
| `host/tests/test_run.py` | 测试 | 2026-08-25 12:49 | 34 项 unittest |
| 当前 `host/run.py` / `host/gui.py` | 源码 | run 2026-08-25 12:47 / gui 2026-08-24 22:55 | 最新工作树 |

> 注：「6 个 P0 缺陷」「停止看门狗/坐标 F8」「界面启动修复（字体/启动）」等具体改动细节来自开发事件记忆，大部分能在源码/备份中得到函数级佐证；如需对外发布精确到 commit 的逐条历史，**建议先 `git init` 并按 `docs/版本整理说明.md` 的分阶段提交固化**。

---

*CHANGELOG 由 ipc-auto-fill 协作团队整理，以可核实文件证据为准；标注「待确认」项需发布前再核对。*
